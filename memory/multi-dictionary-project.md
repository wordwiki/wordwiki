---
name: multi-dictionary-project
description: "APPROVED direction — N dictionaries (separate tables), per-dictionary soft schema loaded as data, shoebox .typ import; survey doc is wordwiki/multi-dictionary-survey.md"
metadata: 
  node_type: memory
  type: project
  originSessionId: 61972dfd-5245-4f6c-8442-1149dcc1ee7b
---

Direction set 2026-07-24: a researcher offered several dictionaries — the gold one a ~30K-entry transcription of RAND into Listuguj (+ some SF). NOT merged into MMO: separate dictionaries, one app server, entries copyable with provenance links. Each dictionary: its OWN assertion table (separate-tables decision is FINAL — dz; wordwiki/multi-dictionary-support.md predates the discussion and is mostly superseded, incl. its dictionary_id sketch), its OWN soft schema loaded as DATA, its own published static site + home content, with cross-dictionary public search (primary first, foreign hits badged + linked over) and cross-site audio via the shared content-addressed stores. End goal: port dz's Java shoebox .typ parser → load an external dictionary and edit it directly (shoebox/toolbox replacement). Import model (dz): .typ import is LITERAL/lenient (raw-rand as-is); cleanup happens IN-SYSTEM via a rand→reshaped-rand dictionary transform (so dictionary creation must be cheap ceremony) — the offline java→python→import-mmo pipeline pattern is retired (import-mmo.ts = completed one-shot, never runs again). Public search (dz 2026-07-24): the CSS-selector home-page search gets REPLACED by an offline JS-index search — the OFFLINE invariant stays (survives server death, works from file://, so index via <script src> not fetch); separable, can land before the multi-dictionary phases. Site-editor requirements brought forward (dz 2026-07-24, survey §2.8): dumps stay THE publish interface — simple json + a few-imports REFERENCE static-site generator is the look-and-feel escape hatch other language projects need (core adoption requirement, not archival nicety); public pages must ALSO render LIVE (preview routes over an in-memory PublishSource) because the site-editor flow is broken without page preview. Site editor itself is still raw ("needs fiddling"). Watson (the researcher, now 90) is MMO's ancestral author — his dictionary of ~25 years ago became shoebox-MMO (java static generator; Sfm.java was its front end) which became wordwiki; his current drop (repo watson/ + watson.txt) is a work-in-flight pipeline (raw Rand queue + Ng/Lk orthography-forked "Final" pairs) that wordwiki's variant lanes reunify. Licensing settled: CC-share-alike like MMO, kept separate, ATTRIBUTION TO WATSON.

PROGRESS: phases 0 (grout: round-trip $style emission, strict $style/$view validator, hand renderer retired from production, fallback maps retired/variants map DELETED, import-mmo gone), 1 (roles: $role in model.ts w/ 7 roles + relationsByRole, schema-roles.ts generic access layer, ALL core role/schema-driven incl. LexemeOps + publish walks + publication-ops rootTable) and 2 (schema-as-data: dict + dict_dict_config pair, schema row synced from literal transitional, checkProposedSchema gate + dump-schema/load-schema CLI + ensure-dict-config pipeline step 4/16; PublishSource fmt v2 EMBEDS the schema (v1 readable); site derivations take schema explicitly; per-dictionary site config as seeded config pairs) COMPLETE on main 2026-07-24. Phase 3 core done 2026-07-24/25 (store map + storeFor/dictionaries, new-dictionary CLI, dict() facade + dotted wordwiki.dicts.* via routeterp dynamicRouteMember, and the PER-DICTIONARY EDITOR — LexemeApp/LexemeEditorApp narrow interfaces, editor route base R as instance state). Phase 5 steps 1-2 done 2026-07-25: sfm.ts (Sfm.java port, watson/ files = permanent zero-problem pins) + sfm-import (tree/flat structure modes, deterministic re-imports, edited-mirror refusal; ALL of RAND imports in ~10s and browses at wordwiki.dicts.randraw.*). RAND's import path = TREE mode with watson/rand-structural.typ (generated by watson/make-structural-typ.ts from an override map — Watson's own .typ is a 14-level record-template VINE, not structure; \ps repetition = sense grouping, opener must BE the parent). Phase 5.3 done 2026-07-25: dictionary-transform.ts (gate, deterministic ids reusing source facts, intoParentField, deep-path wrappers, parsers, deep skipEmpty, unmapped accounting) + load-mapping/dump-mapping/transform CLI + watson/rand-transform.json starter — FULL RAND: 29,097 entries → 246,112 rich assertions in 13s; iteration worklist = 560 citation parse-misses + 98-tuple unmapped tail. PRINCIPLE (dz): rich-rand's structure hews to MMO wherever concepts match — same tags/relation/field names (spl/sub/gls/exa/etx/etr/prn/src/nte/att; shoebox dates as attr 'shoebox-date') — NO gratuitous difference (eases cross-dictionary word copying), but no unnecessary levels for compatibility's sake either (record-level source/pronunciation stay record-level). Residuals listed in the doc's phase STATUS blocks. Regression oracle used throughout: publish --from=dump byte-identical (16,341 pages) — rebuild baseline tree with `wordwiki.ts publish --root=<dir> --from=mmo/li/data/publish-source.json,mmo/sf/...` from a scratch dir. Schema-storage decision CONVERGED: table pair X + X_config, schema JSON in config, no registry table (see doc §3.1). Next: phase 2 (dict_config + schema-as-data).

Doc of record: **wordwiki/multi-dictionary-survey.md** (2026-07-24 four-agent survey) — what's already generic, the hard-coding inventory, choices, and the 6-phase staged plan (0 grout → 1 schema roles → 2 schema-as-data+registry → 3 multi-store+routes → 4 publish+cross-search → 5 .typ import). Key survey facts: schema literal already 100% JSON-serializable; assertion SQL layer fully table-parameterized; DictionaryStore one ctor-arg from multi-instance (assertionTable='dict' at :35); render-entry-meta is already THE public renderer; real work = typed Entry world (choke point: dictionary-store entries/publishedProjection casts), ~11 semantic roles hardwired to MMO relation names, ~14 raw-dict-SQL files, ~163 routes/~150 URL literals, publish content constants. Regression oracle: publish --from=dump byte-identical tree diff.

**Why:** RAND is a historical source; keeping it a unit preserves provenance/scholarly integrity; MMO stays the voiced/vetted primary. Multi-schema makes the tool reusable for other language-preservation projects.

**How to apply:** read the survey doc before touching this; peers consume PROJECTION artifacts, never each other's schemas; leaf MMO reports may stay typed — core (editor/versioning/publication/feeds/search/publish traversal) must be role/schema-driven. Open questions in doc §6 (site editor dz mentioned — not found in repo; schema storage; RAND orthography row; scale of the inline home-page search index). Related: [[fix-orthographies]] (finish production migration first), [[wordwiki-archival-publish-model]], [[pdm-llm-transcription]] (same cherry-picking shape), [[wordwiki-data-licensing]] (per-dictionary license/attribution in the registry).
