---
name: clark-import
description: "Clark 1902 image import as reference dictionary: two-layer OCR/interpretation, textract geometry + LLM text hybrid, rand prior as checker; join hub onto rand/watson for MMO growth"
metadata: 
  node_type: memory
  type: project
  originSessionId: 61972dfd-5245-4f6c-8442-1149dcc1ee7b
---

Clark import project (dz 2026-07-28, doc of record wordwiki/clark-import-design.md): import Clark 1902 ("Rand's Micmac dictionary from phonographic word-lists", Jeremiah S. Clark) from page images as a REFERENCE dictionary in the [[multi-dictionary-project]] model. THE JOIN IS THE POINT: Clark entries become hub starting-points (joined onto rand/watson, some % PDM) that a person copies into MMO — the growth speedup.

MEASURED day one (printed-170 probe): NOT a mechanical reversal of Rand 1888 — weskōdum merges >=4 scattered Rand slips w/ a NEW gloss; wenjāwe regrammaticalized ('leader'→'I lead'); 8/18 probed entries have NO rand match (compiled from Rand's manuscript word-lists); Clark-only apparatus (cf.-refs, P.E.I. dialect notes, place names). Entry granularity = the Mi'kmaq word (like MMO, unlike rand's English slips) → Clark↔rand join is MANY-TO-ONE, Clark = the hub entry. Orthography = Rand lighter: macrons/circumflex kept, breves mostly dropped, tc for ch; plain diacritic folding already joins 10/18.

ASSETS ALL BANKED: 234 tifs imports/Clark (document_id 3); printed 1-172 = scans 39-210 mapped in scanned_page.printed_page_number; textract DONE and loaded as 'Text'-layer bounding_box LINE rows (geometry good, text accent-stripped — dz: unusable as text).

ARCHITECTURE (dz's 3 issues): layer 1 = PHYSICAL transcription only (glyphs+diacritics, *italic* style, line identity; NO language tags — italics carry language; pushback accepted); layer 2 = interpretation, text-only, iterate-freely on cached layer 1; extract substrate (liminal/extract.ts) keys both. Textract's 3 roles: mechanical segmentation (columns/bands/hanging-indent entry starts), bounding boxes (entry ref = union of line boxes — NEVER ask the vision model for coordinates), fold-comparison hallucination check. Rand prior = CHECKER not primer (priming copies the prior over the ink exactly where orthographies differ); second pass w/ candidates only on flagged lines. Entries keep verbatim transcription as first-class field; "See X" lands unresolved, resolved in a later whole-book pass; landing = own `clark` table, machineSync, content-keyed ids.

BUILT: wordwiki/page-transcribe.ts (band transcription over textract geometry, book-generic; MASKED band crops à la PDM — plain rects catch neighbour-column hyphen tails; ~16 lines full-res under the 1568px API downscale; BandInput in extract `input` so per-band facts are in the cache key; alignFolded sequence alignment — the model legitimately drops running heads, index-pairing mis-scores everything) + CLI `transcribe-survey` (--book --pages --model --json --report) + pure-helper tests. Stage plan A survey → B diacritic eval → C 20-30 page dev band + landing → D freeze + full 172. Grammar/place-name sections out of scope (place-names maybe their own prize later).

STAGE A DONE (3 iterations, ~$5.7, clark/transcribe-survey.md): fold-exact 88-96%/page; residual disagreements are TEXTRACT errors (LLM out-reads it); headers merge into one line (harmless). STAGE B DONE (clark/diacritic-eval.md): Opus/Sonnet differential + ink adjudication of all 37 letter-level divergences + 6 MMO hand-ref gold. SONNET WINS: 2.7% vs 4.9% line error rate at 1/5 cost (full book ~$15 vs ~$65) — clean-print property, does NOT transfer to PDM manuscript/binder where Sonnet failed. Production recommendation: DUAL-MODEL gate (agreement ~90% auto-accepts, divergence ~9% = review queue, ~$80/book); known-fuzzy class = worn o-accents (ó/ô), survives even dual-agreement. Layer-2 interpret: Sonnet ≡ Opus on comparable entries. MMO's 6 Clark refs carry hand rtr/rtl — rtl = clark→mm-li seed corpus for a future transliteration pair.

STAGE C BUILT (2026-07-28): wordwiki/clark-import.ts + assembly in page-transcribe.ts; CLI `clark-import --pages=1-25`. Dual-model layer 1 (Sonnet PRIMARY per stage B, Opus gate, divergent lines flagged; llmRetry + band-level parallelism — llm.ts has NO retry of its own), mechanical assembly (GUTTER-DETECTED column split — center-x-vs-page-mid misfiles short indented lines ('abode' bug: right column STARTS left of page-mid); hanging-indent starts; cross-column/page stitch), Sonnet interpret, landing = import-mirror `clark` table (sfm-style wipe+rebuild, contentKeyId ids, '~clark-import', orthography row 'clark') + per-entry bounding groups on Tagging:clark sheet + verbatim stx + unresolved xrf. 881 entries/5,163 assertions from printed 1-25, generation 3 (~$15 first run, re-runs ~free). TWO BUGS the dev band caught: center-x column split misfiled SHORT indented lines ('abode' → wrong column's entry; fix = gutter detection, largest x-gap in line starts) and liminal getDerived used std fs.move = REMOVE-then-rename (concurrent reader sees file vanish; fix = atomic Deno.rename — hit by dual-model runs deriving the same contained crop). KEY JOIN FIX: rand entries were INVISIBLE to clark — rand's headword role holds only watson lanes; added schemaRoles.sourceOrthographyTexts ($sourceOrthography relations = spellings) into entryKeys + spellingsOf (rand index 740k keys). MEASURED: clark→rand same-word 371 pairs, referral 466; 20.3% of clark entries exact-skel collide with rand; 5.9% reach MMO via the landed rand→mcp hub already. clark→dict direct small BY DESIGN — MMO reach = hub composition clark→rand→mcp. 'clark' normalizer in MIKMAQ_NORMALIZERS (rand folding + tc→ch). Dev-band known items: entry-start over-detection (junk fragments), ~11% low-confidence interpretations.

STAGE D DONE (2026-07-28, generation 5): FULL BOOK printed 1-172 → 6,694 entries / 37,715 assertions (glosses 7,659, derivatives 2,049, xrefs 232, notes 794; 1,019 divergent lines flagged; ~$95). Run hardening: llmRetry covers schema-mismatch responses; failed band degrades to textract fallback (one bad response must not kill a book run). Dev-band review first: interpret v2 fixed ENDING-AS-HEADWORD ('ul'/'k' after headword are inflection endings → `endings:` note, 91 landed), I5/2I confusable page numbers → header filter, empty fragments skipped; residual junk ~0.5%. FULL-BOOK JOIN: clark→rand same-word 4,563 pairs (referral 5,424 for a future judge); 27.6% of clark entries exact-skel collide with rand; 8.7% (583) already reach MMO via rand→mcp hub; clark→dict direct 281.

DICTIONARY PICKER (dz 2026-07-28, BUILT): active dictionary = URL-DERIVED ONLY (dz pushback: session copy would fight the URL) — navbar badge peer to orthography switcher; picker = plain navigation to dicts.X.home(); navbar persists across htmx swaps → tiny inline afterSwap hook re-derives badge+scoped links from location.pathname; per-dictionary search + letter-index browse on DictionaryPages; serve heap 8GB (public server limit; 4GB OOMed with rand+clark+MMO workspaces).

MULTI-DICT UX ROUND (dz preview 2026-07-28, all BUILT): (1) ONE RENDERER family — every per-dictionary surface goes through the generalized META renderer/editor (metaEditPage); the classic entryPage look is MMO-typed legacy, nothing per-dictionary points at it; editor headings schema-generic (entryHeading — entrySchema.renderEntrySpellingsSummary reads the DEFAULT schema → 'No spellings' on other dicts). (2) APPROVAL: factExemptFromApproval — in non-default dicts a fact whose whole history is machine/imported (isImportedEvent) is SETTLED (fixes all-pending chips + phantom 'Approve N auto transliterations'; matches the review's pure-import rule; MMO unchanged — machine-pending = auto-transliteration queue). (3) rand images ≥ printed p76 = NO refs yet (binder paused at 75, deferred doc) — not a rendering bug. (4) clark rtr nesting. (5) WORKFLOW EVERYWHERE: Tags/Log/dock generalized by dict param on the wordwiki.* workflow routes (ops via editorAppFor facade; dialogs via dicts.X.lexeme); ensure-workflow-relations CLI copies dict's tdo/log relation JSON into any dictionary schema (applied: rand, clark; clark-import calls it); clark mirror wipe/foreign-check EXEMPT 'tdo'/'log' (human rows survive re-imports, orphan visibly on changed entries). (6) PACKAGING: clark-import.ts + Clark stage prompts moved to mikmaq/ (dz rule); page-transcribe.ts book-generic, transcribeSurvey takes stages from the CLI binary edge. OPEN: rand subentries discussion (transform-shaped, flippable); classic-look retirement dz's call.

NO-AI PROOF MODE (dz 2026-07-28, BUILT): flag file `no-llm-calls` in the run cwd (beside the credential file) or LIMINAL_NO_LLM=1 → AnthropicLlm.extract THROWS loudly (model + prompt head) at the actual-work site; cache hits never reach it. Purpose: re-run the full migration in a fresh container over the shared content store and PROVE zero AI spend. Documented in importWordWikiV1Db.sh header. Verified: cached survey passes ($0.00), uncached (haiku model / opus-v2-interpret) throws. NOTE: importer/textract.ts is DORMANT (imports nonexistent ./db.ts etc., doesn't compile) but DO NOT DELETE (dz 2026-07-28): it gets updated to the revised model at the NEXT book import — when revived, wire assertLlmCallsAllowed into its aws call sites so the no-AI proof mode covers textract too. No live textract runner today; migration never calls AWS.

Interaction: Clark landing reshuffles the rand↔dict referral band → MORE reason [[transliteration-pairs]] deferred runs stay deferred.
